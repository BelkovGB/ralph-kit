# Хук копируют в `.git/hooks/pre-commit`, где его запускает оболочка: CRLF в
# строке shebang ломает запуск. Каталог `templates/` в проект не копируют,
# поэтому правило лежит рядом с файлом, а не в корневом `.gitattributes`.
pre-commit text eol=lf
